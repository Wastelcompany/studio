{
  "entities": {
    "Company": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Company",
      "type": "object",
      "description": "Represents a single company dossier for a facility. It contains high-level information about the company.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Company entity."
        },
        "userId": {
          "type": "string",
          "description": "Reference to the User who owns this company."
        },
        "name": {
          "type": "string",
          "description": "A user-defined name for the company or facility analysis."
        },
        "address": {
          "type": "string",
          "description": "An optional address of the company."
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp when the company was created.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "userId",
        "name"
      ]
    },
    "SubstanceInventoryItem": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "SubstanceInventoryItem",
      "type": "object",
      "description": "Represents a single chemical substance present in a specific company's inventory.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the substance."
        },
        "productName": {
          "type": "string",
          "description": "The name of the product or substance."
        },
        "casNumber": {
          "type": "string",
          "description": "The Chemical Abstracts Service (CAS) number."
        },
        "hStatements": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Array of Hazard statements (H-statements)."
        },
        "sevesoCategoryIds": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Array of Seveso category IDs the substance falls into."
        },
        "arieCategoryIds": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Array of ARIE category IDs the substance falls into."
        },
        "isNamedSubstance": {
          "type": "boolean",
          "description": "Flag indicating if it is a named substance."
        },
        "namedSubstanceName": {
          "type": "string",
          "description": "The name of the substance if it's a named one."
        },
        "quantity": {
          "type": "number",
          "description": "The quantity of the substance present, in tons."
        }
      },
      "required": [
        "id",
        "productName",
        "hStatements",
        "sevesoCategoryIds",
        "arieCategoryIds",
        "isNamedSubstance",
        "quantity"
      ]
    },
    "UserProfile": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "User Profile",
      "type": "object",
      "description": "Represents a user's profile information.",
      "properties": {
        "uid": {
          "type": "string",
          "description": "The user's unique ID from Firebase Authentication."
        },
        "email": {
          "type": "string",
          "format": "email",
          "description": "The user's email address."
        },
        "displayName": {
          "type": "string",
          "description": "The user's display name."
        },
        "createdAt": {
          "type": "string",
          "format": "date-time",
          "description": "Timestamp when the user profile was created."
        }
      },
      "required": [
        "uid",
        "email",
        "createdAt"
      ]
    }
  },
  "auth": {
    "providers": [
      "password"
    ]
  },
  "firestore": {
    "structure": [
      {
        "path": "/users/{userId}",
        "definition": {
          "entityName": "UserProfile",
          "schema": {
            "$ref": "#/backend/entities/UserProfile"
          },
          "description": "Stores public profile information for each user. The document ID is the same as the user's auth UID. Profile creation is allowed upon signup, but all subsequent access requires email verification. The verified admin user (post@wastelcompany.eu) has read access to this entire collection."
        }
      },
      {
        "path": "/companies/{companyId}",
        "definition": {
          "entityName": "Company",
          "schema": {
            "$ref": "#/backend/entities/Company"
          },
          "description": "Stores company profile information. Documents in this collection contain a `userId` field to indicate ownership. Access is restricted to the verified owner of the document."
        }
      },
      {
        "path": "/companies/{companyId}/inventory/{substanceId}",
        "definition": {
          "entityName": "SubstanceInventoryItem",
          "schema": {
            "$ref": "#/backend/entities/SubstanceInventoryItem"
          },
          "description": "Contains chemical substances belonging to a specific company. Access is restricted to the verified owner of the parent company.",
          "params": [
            {
              "name": "companyId",
              "description": "The unique identifier of the parent company."
            },
            {
              "name": "substanceId",
              "description": "The unique identifier for the substance inventory item."
            }
          ]
        }
      }
    ],
    "reasoning": "The Firestore structure is designed around a top-level `companies` collection. This model was chosen to simplify querying for company data and to decouple companies from users, allowing for potential future features like sharing companies between users. Security is maintained by embedding a `userId` field in each company document, which is then used in Firestore Security Rules to enforce ownership. All subcollections, such as `inventory`, inherit access rights by checking the ownership of the parent company document, ensuring data remains secure and private to the owner. A `users` collection is also added to store user profile information, with special read-access for an admin role. Access to all data now requires users to have a verified email address."
  }
}
